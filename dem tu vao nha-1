#include <iostream>
using namespace std;

bool lotkhe(int l1, int l2, int k1, int k2) {
  if ((l1 < k1) && (l2 < k2)) {
    return true;
  } else if (l1 < k2 || l2 < k1) {
    return true;
  }
  return false;
}

int main() {
  int a, b, c, x, y;
  cin >> a >> b >> x >> y;

  bool lot = false;
  if (lotkhe(a, b, x, y)) {
    lot = true;
  } else if (lotkhe(b, c, x, y)) {
    lot = true;
  } else if (lotkhe(a, c, x, y)) {
    lot = true;
  }
  if (lot) {
    cout << "Yes";

  } else {
    cout << "No";
  }
}
